<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_scroll_gradient"
    tools:context=".DeletedNewsActivity">

    <!-- Toolbar dengan menu -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/topAppBar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        app:navigationIcon="@drawable/ic_back"
        android:background="@android:color/transparent"
        android:theme="@style/ThemeOverlay.Material3.Dark.ActionBar"
        app:title="News Arsip"
        app:titleCentered="true"
        app:titleTextColor="@android:color/white"
        app:navigationIconTint="@android:color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Container untuk filter chips dan refresh -->
    <LinearLayout
        android:id="@+id/filterContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingVertical="8dp"
        app:layout_constraintTop_toBottomOf="@+id/topAppBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Title untuk filter -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="8dp"
            android:text="Filter Berdasarkan:"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold"/>

        <!-- ChipGroup untuk filter -->
        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chipGroupFilter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            app:singleSelection="true"
            app:selectionRequired="true"
            app:chipSpacingHorizontal="8dp"
            app:chipSpacingVertical="0dp">

            <!-- Chip Semua -->
            <com.google.android.material.chip.Chip
                android:id="@+id/chipAll"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Semua"
                android:checked="true"
                app:chipIcon="@drawable/ic_all"
                app:chipIconTint="@android:color/white"
                app:chipBackgroundColor="@color/chip_background_selector"
                app:checkedIconVisible="false"/>

            <!-- Chip Promo Terhapus -->
            <com.google.android.material.chip.Chip
                android:id="@+id/chipDeletedPromo"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Promo Terhapus"
                app:chipIcon="@drawable/ic_delete"
                app:chipIconTint="@android:color/white"
                app:chipBackgroundColor="@color/chip_background_selector"
                app:checkedIconVisible="false"/>

            <!-- Chip Promo Kadaluwarsa -->
            <com.google.android.material.chip.Chip
                android:id="@+id/chipExpiredPromo"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Promo Kadaluwarsa"
                app:chipIcon="@drawable/ic_expired"
                app:chipIconTint="@android:color/white"
                app:chipBackgroundColor="@color/chip_background_selector"
                app:checkedIconVisible="false"/>

            <!-- Chip Hunian Terhapus -->
            <com.google.android.material.chip.Chip
                android:id="@+id/chipDeletedHunian"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hunian Terhapus"
                app:chipIcon="@drawable/ic_hunian"
                app:chipIconTint="@android:color/white"
                app:chipBackgroundColor="@color/chip_background_selector"
                app:checkedIconVisible="false"/>

            <!-- Chip Proyek Terhapus -->
            <com.google.android.material.chip.Chip
                android:id="@+id/chipDeletedProyek"
                style="@style/Widget.Material3.Chip.Filter"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Proyek Terhapus"
                app:chipIcon="@drawable/ic_proyek"
                app:chipIconTint="@android:color/white"
                app:chipBackgroundColor="@color/chip_background_selector"
                app:checkedIconVisible="false"/>

        </com.google.android.material.chip.ChipGroup>

        <!-- Layout untuk item count dan refresh -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="2dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Item count -->
            <TextView
                android:id="@+id/tvItemCount"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Total: 0 item"
                android:textColor="@android:color/white"
                android:textSize="12sp"/>

            <!-- Refresh Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnRefresh"
                android:layout_width="wrap_content"
                android:layout_height="38dp"
                android:text="Refresh"
                android:textSize="12sp"
                android:textColor="@android:color/white"
                app:icon="@drawable/ic_refresh"
                app:iconSize="16dp"
                app:iconTint="@android:color/white"
                style="@style/Widget.Material3.Button.OutlinedButton"
                app:strokeColor="@android:color/white"
                app:strokeWidth="1dp"/>

        </LinearLayout>

    </LinearLayout>

    <!-- SwipeRefreshLayout -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshLayout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/filterContainer"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Konten -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent">

            <!-- Progress Bar -->
            <ProgressBar
                android:id="@+id/progressBar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:visibility="gone" />

            <!-- Empty State -->
            <TextView
                android:id="@+id/tvEmptyState"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="Tidak ada arsip berita"
                android:textColor="@android:color/white"
                android:textSize="16sp"
                android:visibility="gone" />

            <!-- RecyclerView -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerDeletedNews"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:padding="8dp"
                android:scrollbars="vertical"
                tools:listitem="@layout/item_news_card" />

        </RelativeLayout>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        app:menu="@menu/bottom_nav_menu"
        app:labelVisibilityMode="labeled"
        app:itemIconTint="@color/bottom_nav_selector"
        app:itemTextColor="@color/bottom_nav_selector"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:itemActiveIndicatorStyle="@style/BottomNav" />

</androidx.constraintlayout.widget.ConstraintLayout>
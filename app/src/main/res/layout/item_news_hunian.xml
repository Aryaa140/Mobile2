<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardElevation="4dp"
    app:cardCornerRadius="8dp"
    app:cardBackgroundColor="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- ImageView -->
        <ImageView
            android:id="@+id/imgHunian"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:scaleType="centerCrop"
            android:visibility="visible"
            android:src="@drawable/ic_hunian"
            android:contentDescription="Gambar Hunian"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingStart="12dp">

            <!-- Nama Hunian -->
            <TextView
                android:id="@+id/tvNamaHunian"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Nama Hunian"
                android:textStyle="bold"
                android:textSize="16sp"
                android:textColor="@android:color/black"
                android:maxLines="2"
                android:ellipsize="end"/>

            <!-- Nama Proyek -->
            <TextView
                android:id="@+id/tvNamaProyek"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Proyek: "
                android:textSize="14sp"
                android:textColor="#666666"
                android:maxLines="1"
                android:layout_marginTop="4dp"/>

            <!-- Luas -->
            <TextView
                android:id="@+id/tvLuas"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Luas: "
                android:textSize="14sp"
                android:textColor="#666666"
                android:maxLines="1"/>

            <!-- Penginput -->
            <TextView
                android:id="@+id/tvPenginput"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Penginput: "
                android:textSize="14sp"
                android:textColor="#666666"
                android:maxLines="1"/>

            <!-- Status -->
            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Status: Ditambahkan"
                android:textSize="14sp"
                android:textColor="#666666"
                android:maxLines="1"/>

            <!-- Timestamp -->
            <TextView
                android:id="@+id/tvTimestamp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="1 jam lalu"
                android:textSize="12sp"
                android:textColor="#666666"
                android:maxLines="1"
                android:layout_marginTop="2dp"/>

        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>